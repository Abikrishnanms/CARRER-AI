# Enrichment Service dependencies
aiokafka==0.12.0
motor==3.6.0
pydantic==2.10.6
httpx==0.28.1
python-dotenv==1.0.1
# ML salary estimator (optional — service works without a trained model)
scikit-learn==1.6.1
pandas==2.2.3
numpy==1.26.4
# spaCy for NER-based skill extraction (optional, falls back to regex)
# spacy==3.8.3
